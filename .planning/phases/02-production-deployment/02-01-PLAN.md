---
phase: 02-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server.ts
  - render.yaml
  - package.json
  - package-lock.json
autonomous: true

must_haves:
  truths:
    - Server pre-warms data on startup by running the fetch pipeline once
    - Hourly cron job refreshes cached result without requiring HTTP requests
    - GET /api/coldest returns instantly from in-memory cache when cache is warm
    - GET /api/coldest response includes Cache-Control: public, max-age=60 header
    - First request after cold start triggers a live fetch (cache is null), then serves it
  artifacts:
    - src/server.ts with node-cron integration and in-memory cachedResult
    - render.yaml at project root
  key_links:
    - node-cron schedule triggers refreshData() every hour at :00
    - cachedResult in-memory variable serves all /api/coldest requests instantly
---

<objective>
Refactor server.ts to run the data fetch pipeline on a background hourly schedule instead of per-request,
add Cache-Control headers, install node-cron, and add render.yaml for Render deployment config.

Purpose: Render free tier requires fast HTTP responses. Currently /api/coldest takes 30–90s to respond
because it fetches live data on every request. After this plan, responses will be instant (return
in-memory cached result), and the pipeline refreshes silently every hour via cron.

Output: Modified src/server.ts, new render.yaml at project root.
</objective>

<execution_context>
@/Users/gguja/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gguja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/server.ts
@src/types.ts
@src/finder.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install node-cron and refactor server.ts for background refresh</name>
  <files>src/server.ts, package.json, package-lock.json</files>
  <action>
    1. Install dependencies:
       npm install node-cron
       npm install --save-dev @types/node-cron

    2. Rewrite src/server.ts with these changes (all other logic stays the same):

       Add import:
         import cron from 'node-cron';

       Add a type alias and in-memory cache variable just below the PORT declaration:
         type CachedResult = ReturnType<typeof findColdestPlaces> & { lastUpdated: string };
         let cachedResult: CachedResult | null = null;

       Add a refreshData function before the createServer() call:
         async function refreshData(): Promise<void> {
           console.log('[cron] Refreshing coldest places data...');
           const observations = await fetchAllObservations();
           const result = findColdestPlaces(observations);
           cachedResult = { ...result, lastUpdated: new Date().toISOString() };
           console.log(`[cron] Refresh complete. ${cachedResult.totalStations} stations, ${Object.keys(cachedResult.sources).join('/')} sources.`);
         }

       Modify the /api/coldest handler:
         - If cachedResult !== null: respond immediately with it (do NOT call fetchAllObservations)
         - If cachedResult === null (cold start, no cache yet): call await refreshData() then respond with cachedResult
         - On the 200 response, add header: 'Cache-Control': 'public, max-age=60'
         - Keep the existing 500 error handler structure with try/catch

       After the server.listen() call, add:
         cron.schedule('0 * * * *', () => { refreshData().catch(console.error); });
         console.log('[cron] Hourly refresh scheduled (fires at :00 each hour)');
         refreshData().catch(console.error); // pre-warm cache on startup

    3. Run: npx tsc --noEmit
       Fix any TypeScript errors before proceeding.
  </action>
  <verify>npx tsc --noEmit exits with code 0 and zero errors</verify>
  <done>
    TypeScript compiles cleanly; server.ts imports node-cron; cachedResult pattern
    is in place; /api/coldest handler serves from cache when warm; Cache-Control header present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add render.yaml and verify production build</name>
  <files>render.yaml</files>
  <action>
    1. Create render.yaml at the project root with this exact content:

       services:
         - type: web
           name: coldestplace
           env: node
           buildCommand: npm install && npm run build
           startCommand: npm start
           plan: free

    2. Run: npm run build
       This runs tsc and outputs to dist/. Must succeed with zero errors.

    3. Verify dist/server.js exists after the build.
  </action>
  <verify>
    1. npm run build exits with code 0
    2. dist/server.js exists: [ -f dist/server.js ] && echo "OK"
    3. render.yaml exists at project root: [ -f render.yaml ] && echo "OK"
  </verify>
  <done>Production build succeeds; render.yaml present at project root.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes with zero errors
- npm run build passes (dist/server.js created)
- render.yaml exists at project root
- src/server.ts contains node-cron import, cachedResult variable, refreshData function
- Cache-Control header is set on /api/coldest 200 responses
- node-cron and @types/node-cron appear in package.json
</verification>

<success_criteria>
- node-cron installed with TypeScript types
- server.ts serves /api/coldest from in-memory cache (instant responses when warm)
- server.ts pre-warms cache on startup and refreshes every hour
- Cache-Control: public, max-age=60 on API responses
- Production build compiles cleanly
- render.yaml ready for Render deployment
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-deployment/02-01-SUMMARY.md`
</output>
